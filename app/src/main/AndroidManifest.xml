<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shanks.com.fitness">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fitness"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fitness"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.Splash"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.Login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.Register"
            android:label="Register" />
        <activity android:name=".activity.Dashboard" />
        <activity android:name=".activity.Profile" />
        <activity android:name=".activity.DietChart" />
        <activity android:name=".activity.Steps" />
        <activity android:name=".activity.MealScheduler" />
        <activity android:name=".activity.CalorieBurn" />
        <activity android:name=".activity.ForgotPassword" />
        <activity android:name=".activity.MealTypeScheduler" />
        <activity android:name=".activity.DietType" />
        <activity
            android:name=".activity.MainDashboard"
            android:label="@string/title_activity_main_dashboard"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>